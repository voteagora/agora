{
  "name": "ens-agora",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ voteagora/ens-agora",
    "create-local": "graph create --node http://localhost:8020/ voteagora/ens-agora",
    "remove-local": "graph remove --node http://localhost:8020/ voteagora/ens-agora",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 voteagora/ens-agora",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": {
    "@goldskycom/cli": "^0.2.12",
    "matchstick-as": "0.5.0"
  }
}
